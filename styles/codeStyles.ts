import { DARK_THEME, LIGHT_THEME } from "./code/languages/default";

const codeStyles = {
  light: {
    default: LIGHT_THEME,
  },
  dark: {
    default: DARK_THEME,
  },
};

export default codeStyles;
